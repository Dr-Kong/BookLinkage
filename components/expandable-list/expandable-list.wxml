<!--components/list/list.wxml-->
<view class='container'>
	<!-- Title -->
	<view class='title title-class' bindtap="expand">{{title}}</view>
	<!-- Contents -->
    <view class='contents' wx:if="{{on}}">
		<slot/>
	</view>
</view>
